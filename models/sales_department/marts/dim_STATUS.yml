version: 2

models:
  - name: dim_STATUS
    description: "Dimension table that contains information about sales STATUS."
    columns:
      - name: STATUSID
        description: "Primary key for Status records."
      - name: NAME_STATUS
        description: "Status name."
